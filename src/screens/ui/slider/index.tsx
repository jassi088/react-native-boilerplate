import { View } from 'react-native'
import React from 'react'
import { Text } from '@/components/text'

export const Slider = () => {
  return (
    <View>
      <Text label={'Slider'} />
    </View>
  )
}