import { View } from 'react-native'
import React from 'react'
import { Text } from '@/components/text'

export const InputDate = () => {
  return (
    <View>
      <Text label={'InputDate'} />
    </View>
  )
}